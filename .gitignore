.DS_Store
*.log
.claude/
__pycache__/

# Large local-only assets — screen recordings and the raw ACLED exports the
# xlsx is built from. Kept out of history for size; they live on disk only.
*.mov
raw-*.csv
*.MP4
*.mp4
_debug-trash.json
_debug-copy.json

# Scratch screenshots from headless probes — local only.
shots/

# ACLED-licensed data — NEVER commit. ACLED forbids public access to its content,
# so every workbook (and the raw exports above) stays on disk only. events.json is
# the single committed derivative, pending ACLED's written OK (see wiki/Data.md).
*.xlsx
_debug-misclassified.json
_debug-fonts/
