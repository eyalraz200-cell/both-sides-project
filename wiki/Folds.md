# Folds

`@foldN` is the canonical, 1-indexed on-screen numbering. **It is off by one from the
HTML id: `@foldN` = `id="page-(N-1)"`.** Verified against `project.html`'s sections and
`PAGES[]` in `js/core.js` — there are **16 folds** (`page-0` … `page-15`), matching the
16 entries in `PAGES[]` and the 16 `.text-section`s in `project.html`. Every title block
is its own fold: there is no "b" suffix and no shared id. Slots 10 and 11 of `PAGES[]`
are both `drawPage8` — @fold11 fires the bridge glide and @fold12 plays over it.

> Older notes describe 11–15 folds, and anything mentioning `@fold10b` / `#page-9b` is
> stale — that fold is `@fold11` (`#page-10`), and every fold after it moved up one.
> The code has 16, ending at `#page-15`. This page is the source of truth.

## The table

| `@foldN` | id | `PAGES[]` draw fn | Title (Hebrew, truncated) | What plays |
|---|---|---|---|---|
| `@fold1` | `page-0` | `drawPage1` | קיצוניים משני הצדדים (cover) | Hero overlay (logo/title/subtitle) + the fixed dot columns; scroll-lag damping. **Idle scroll cue:** 2s after the entrance finishes with no scroll, the dot columns (decorative dots + the six group swatches) pulse in a wave from the top row down to the bottom (`page0CueRun`, js/fold1-intro.js). The pulse **shrinks** each dot rather than growing it — trough scale 0.3 (a 7px dot to ≈2.1px), 940ms per dot, 22.5ms per row, so ≈42 rows are mid-pulse at once and the wave is a broad swell rather than a thin band (`PAGE0_CUE_SCALE` / `PAGE0_CUE_DOT_MS` / `PAGE0_CUE_ROW_STAGGER_MS`). Both kinds of dot reach the same trough: the pulse suppresses `.group-swatch`'s CSS `transition: transform 260ms ease` (kept for @fold9's filter-hover flip) for the duration of a run, otherwise the six swatches lag the shrink and bottom out short of 0.3 while the decorative dots hit it exactly. Repeats every 3s; the first scroll/wheel/touchmove cancels it for good — the cancel **eases out**, it does not snap: each dot ramps from the scale it is drawn at back to rest over `PAGE0_CUE_EXIT_MS` (260ms, p9Ease), and `updateGroups`'s own decorative-dot write stands down while that plays (`page0CueOwnsDots`). Writing the rest transform in one frame, as it used to, popped a dot caught at the 0.3 trough straight back to full size on the very first scroll. Skipped under `prefers-reduced-motion` |
| `@fold2` | `page-1` | `drawBackground` | בשנים האחרונות התגבשו בישראל… | `fold2Trigger`, 4 beats: dots shrink → fly into the two 4×3 camp grids → each camp header types in |
| `@fold3` | `page-2` | `drawBackground` | בשני המחנות קיימות קבוצות… | `fold3Trigger`, 3 beats: filler rects shrink away → surviving rect per row flies into one column → group labels type in |
| `@fold4` | `page-3` | `drawFoldSplit` | בשל פעילותן בשטח… | `fold6Trigger`: the 6 groups glide into the persistent two-column mini-legend; the camp headers **un-type** (see below). On desktop the group labels un-type once the glide has landed (`fold6LabelUntypeTrigger`, chained off `onSettle`), leaving bare swatches — hovering either legend column types them all back in (`fold6LabelHoverTrigger`) |
| `@fold5` | `page-4` | `drawBackground` | כל ריבוע מייצג פעולה פוליטית של אחת הקבוצות… | `squaresRevealTrigger`: 8 grey sample squares grow in at center |
| `@fold6` | `page-5` | `drawBackground` | הנתונים לקוחים מגוף המחקר… (ACLED, with a visible external link in the card; **mobile only** ends the same line with «- פרטים במקרא.», via the `.copy-desktop` / `.copy-mobile` span pair — desktop's sentence is unchanged) | **Mobile also JUMPS the closed מקרא pill once on this crossing** (`fold6MLegendJump` / `checkMLegendJump`, js/groups.js): it grows `FOLD6_MLEGEND_JUMP_PX` (52) taller and settles back over `FOLD6_MLEGEND_JUMP_MS` (700), base pinned to the bottom edge, **width untouched**, the small line and the title holding their own gap from the card's TOP so the extra height opens up beneath them. It **darkens** as it goes (`FOLD6_MLEGEND_FLASH_ON`, to `FOLD6_MLEGEND_FLASH_COLOR` #e6e4ec over `FOLD6_MLEGEND_FLASH_MS` 640 on its own clock, lerped into `--mlg-fill` from whatever tint the card is wearing) — the pill nudging itself like a waiting notification, pointing at the card's new «פרטים במקרא». A wall-clock beat, forward crossings only, skipped while the panel is open. `acledNoteTrigger`: the title «איסוף הנתונים» and then the ACLED source note **type in** character by character as one continuous stream (`FOLD6_NOTE_BEATS`), with a **vertical hairline** down the block's right edge growing and shrinking with it, then **un-type from the end** on @fold7's crossing (`fold6NoteUntypeTrigger`, desktop only; the title and the rule stay, hovering the legend or the note itself types the body back) under the mini-legend (on mobile: inside the מקרא panel) |
| `@fold7` | `page-6` | `drawFold7` | **desktop** ריחוף העכבר מעל ריבוע חושף את תיאור הפעולה וממחיש את סדר גודלה. · **mobile** לחצו והחזיקו את האצבע על ריבוע כדי לחשוף את תיאור הפעולה ולראות את סדר גודלה. | **The hover demo**, and the fold the square labels type in on — the fold that preceded it («כל ריבוע מייצג פעולה פוליטית…») was removed once @fold5's copy said the same thing, so its `fold7LabelTrigger` (this card at 0.5) moved here. Everything else hangs off this fold's **own established crossing**: once once the card's bottom has reached the tooltip's own top edge plus `FOLD8_TOOLTIP_CLEARANCE_PX` (**−20px** — negative, so the card may still overlap that edge by 20px when the fold fires), `fold8SquareDimTrigger` dims the 7 non-demo squares so the demo dot is singled out, `fold8TooltipTrigger` grows + types the demo tooltip (date + description, unchanged animation) and `fold8DemoGrowTrigger` swells square 0 from its 8px rest side to `FOLD8_DEMO_GROW_PX` — «סדר גודלה», the two reading as one gesture. Each beat waits its own dead time after that one crossing before starting — the swell leads (`FOLD8_DEMO_GROW_DELAY_MS` 0, over `FOLD8_DEMO_GROW_MS` 350ms) and the tooltip follows 250ms later (`FOLD8_TOOLTIP_DELAY_MS`, a delay at the head of its wall-clock sequence, then `FOLD8_GROW_MS` 400ms of grow-and-hold; the swell's delay is a `{start, len}` window on the swell trigger's raw progress read through `fold8DemoGrowT()` — never `fold8DemoGrowTrigger.currentT()`, which still carries the delay). The swell is **centred on the dot** (`growOffset` in js/update-groups.js shifts the square back by half the extra size, since `.fold6-square` is anchored top-left on its zero-size wrap; the offset fades out with `resizeT` so the fly's own geometry is untouched), and the tooltip is positioned off the square's **edges**, not its centre, so its 5px gap is constant at any size. The grown side is the size the @fold8 fly then resizes *from*, so the square never snaps back before departing. Fully reversible. **The demo gesture, per breakpoint.** Desktop: a drawn arrow (`fold7CursorEl`) glides from the 8 squares' centre to square 0 on `fold7CursorTrigger` (`FOLD7_CURSOR_MS` 370), and the swell / dim / tooltip each wait `fold7CursorDelayMs()` (the glide × `FOLD7_CURSOR_ARRIVE_FRAC`) — then any square's hover opens its own tooltip (`fold7SquareHover`). Mobile: the same glide carries the picker's own press-and-hold, played for the reader: a **pointing-hand** cursor (`fold7TouchEl`) and a small **magnifying glass** in front of the dot (`fold7LoupeEl`, `.p7-loupe` look, `FOLD7_TOUCH_LOUPE_PX` 56, `FOLD7_TOUCH_ZOOM` 1.2, painted by `fold7PaintLoupe` from the squares' live rects). The glass **fades in** (`FOLD7_TOUCH_FADE_IN_MS` 400, `p9Ease`), **rests at its start** (`FOLD7_TOUCH_START_REST_MS` 780 — dead time at the head of `fold7CursorTrigger` on mobile via `fold7CursorTotalMs()`, glide re-eased over the rest, and included in `fold7CursorDelayMs()`), glides, **rests on the dot** (`FOLD7_TOUCH_END_REST_MS` 800) and **fades** (`FOLD7_TOUCH_FADE_MS` 400). The hand fades on its own clock (`FOLD7_TOUCH_POINTER_HOLD_MS` 1390 / `FOLD7_TOUCH_POINTER_OUT_MS` 300). After the demo the reader's own **press-and-hold mirrors the timeline picker exactly** (js/groups.js `fold7Hold*`): touch events, `P7_LONGPRESS_MS` / `P7_LONGPRESS_SLOP_PX` with re-anchoring, the picker-size glass (`fold7UserLoupeEl`: `P7_LOUPE_SIZE` / `P7_LOUPE_LIFT_PX`, but a gentler `FOLD7_HOLD_ZOOM` 1.5× instead of the picker's 4×) repainted every frame at the finger, nearest square within `P7_INSPECT_SNAP_PX` picked through `fold7SquareHover`, touchmove claimed only while live, and the docked frame dodging to just below the squares (`tooltipAvoidPx`'s @fold7 branch, js/fold8-tooltip.js). Release hides the glass and keeps the last square. `fold7CursorDelayMs()` applies on both breakpoints. **Mobile differs twice over.** The copy is a different sentence, not a reworded one — a phone presses and holds rather than hovers — carried by a `.copy-desktop` / `.copy-mobile` span pair in the `<h2>` swapped at the 600px breakpoint (style.css), `display:none` so the hidden one adds nothing to `.text-card-frame`'s `fit-content` width. And the crossing is not solved: `fold8TooltipCardFrac()` returns the flat **`FOLD8_MOBILE_CARD_FRAC`** (`var`, **0.15**, js/groups.js — low on purpose, since the fraction is compared against the card's TOP, so 0.15 holds the demo back until the card has nearly cleared the top of the screen) because the docked frame sits at a fixed spot with nothing to collide with. That fraction multiplies `window.innerHeight`, which on a phone **changes as the URL bar collapses**, so the line this fold fires on moves mid-scroll — the first thing to check if the fold ever reads as firing twice. The tooltip itself is a docked frame above the timeline — see [Timeline](Timeline.md#hover) **Fake cursor + hover (desktop only).** On the demo's crossing a drawn pointer (`.fold7-cursor`, a direct `.layout` child) fades in at the 8 squares' centre and glides to square 0, the top-left dot (`fold7CursorTrigger`, `FOLD7_CURSOR_MS` 370, an arrow `FOLD7_CURSOR_SIZE_PX` 24px tall whose box hugs the glyph, hot spot `FOLD7_CURSOR_TIP`, js/groups.js), then waits `FOLD7_CURSOR_HOLD_MS` and fades out over `FOLD7_CURSOR_OUT_MS` (wall-clock from landing). The swell, the dim and the tooltip are **not chained** to it; each waits `fold7CursorDelayMs()` (= `FOLD7_CURSOR_MS × FOLD7_CURSOR_ARRIVE_FRAC` 0.5, 0 on mobile) on the same crossing: the swell via `fold8DemoGrowDelayMs()`, the dim via `fold8SquareDimT()`, the tooltip via `fold8SeqDelayMs` (js/fold8-tooltip.js). Once the cursor has arrived — and on through @fold8, until the squares start flying to the timeline (`fold9FlyTrigger`) — hovering any of the 8 squares swells it and opens the tooltip for its event (`fold7SquareHover`, a window `mousemove` hit-test with a 6px pad; the overlay is under the text column, so the squares get no pointer events). The previous square shrinks back (`fold7SquareHoverTriggers`, `fold7DemoSuppressTrigger`), the cursor hides, and a hover restart keeps only the 250ms tooltip delay. **Leaving the squares keeps the last hovered one.** Whichever square is active when the fly starts gets square 0's flight treatment: the callout swings from above to below it as it travels (`fold8FlyMoveT` rides the active square), then holds and shrinks away as usual. The desktop fill is lerped end to end between the grey's fill and the group's own fill (`setTooltipColor(el, color, fill)`, js/core.js), so the settlers' grey→orange-brown never passes through olive. The tooltip hangs toward the dot's **camp side** — left-camp dots open it to the left (`.is-mirrored`), right-camp dots to the right (`fold8TooltipSide`, desktop). On a hover the swell **animates** (350ms, like a timeline dot's), but the tooltip appears already grown and typed and **never slides**: `fold8PositionTooltip` anchors it to the square's final swollen size around its centre. Every square **rests at 8px**; only the active one (square 0 in the demo, or the hovered square) swells, to a size that tells how many people **its description** says took part — a scale for these 8 only (`FOLD7_SQUARE_SIZES`, js/groups.js), not the crowd tiers (7 of the 8 events have no crowd figure): «מאות» 20px, «עשרות» 14px (the demo's size), «שני» or no number 10px. Scrolling back above the crossing resets all of it, so the demo replays. |
| `@fold8` | `page-7` | `drawFold9` | צבע הריבוע מציין את הקבוצה שביצעה את הפעולה.<br>ניתן לסנן קבוצות באמצעות המקרא. (two lines, a hard `<br>` — the only title block with one) | `fold9Trigger` colors **all 8 squares** + the tooltip (desktop: the fill; mobile: the dashed border); `fold9FlyTrigger` flies them to their real per-event dots — the 4 earliest events per side, pinned by row id (`FOLD6_SQUARE_ROW_IDS`, see [Groups-and-Legend](Groups-and-Legend.md#the-8-sample-squares)). **Desktop:** the demo callout swings from hanging above square 0 to hanging below it (`.is-flipped`) as that square travels, so it is angled down when it lands on the timeline — driven by `fold8FlyMoveT`, square 0's beat-2 progress, **not** by `fold9FlyTrigger` as a whole (whose first `FOLD9_FLY_RESIZE_SPAN` is color+resize, before the square has moved) |
| `@fold9` | `page-8` | `drawPage7` | *(no title — `page7-scrub`)* | The pinned real timeline — desktop: vertical centre axis, rows are dates; mobile: horizontal bottom axis — see [Timeline](Timeline.md). Legend rows are clickable here and only here: a click filters that group out of the graph (`p7FilterToggle`, desktop only). **Mobile tail beat:** once the fill reaches the last event the over-tall field **squashes vertically** — y only, so every dot stays beside its own date and the dots and the dates spread across the axis together — giving a view of the whole timeline before @fold10's title block rises (`p7ZoomOutSync` / `p7ZoomOutYScale`; see [Timeline](Timeline.md#the-end-of-fill-zoom-out-mobile--p7zoomoutyscale-p7zoomoutsync)) |
| `@fold10` | `page-9` | `drawPage7` | המחנות נבדלים במספר הפעולות שביצעו, אך פעולה אחת עשויה להתבצע בידי אדם יחיד, ואחרת בהשתתפות מאות אלפי אנשים. | **The size grid.** The same `drawPage7` canvas with `p7Grid.on` — the trigger is this card's top crossing **mid-screen**, the house 0.5 (`checkFold10Grid`, js/groups.js — a `watchCardThreshold` shim over `p7SizeGridSet` — it fires on **mobile too**; `p7SizeGridOnPage` in page7.js only re-syncs it on re-entry from below — **on mobile as well as desktop**, and it snaps the grid off past @fold12). Every dot on screen morphs to its crowd tier (`P7_GRID_TIER_CELLS`) and re-packs into a gap-exact block per camp while the year axis and the timeline undraw the way they already do off @fold9. One choreographed ms clock (`p7MorphTotalMs()`: flight, then size in tier waves) on `p9Ease`, blending centre **and** size — **nothing fades**; hover tooltip + dim work here. Scrolling back to @fold9 plays the exact mirror; **downward the grid stays on** — @fold12's bridge glide flies the dots out of it (see that row). Geometry and the baked values in [Timeline](Timeline.md#the-size-grid--p7sizegridset-page7js). The card sits **below centre** — **16vh** on desktop (`#page-9`: `padding-top: 16vh` + `min-height: 116vh`), a nudge down so the grid starts after the year axis has finished filling, and **55vh / 155vh** under the 600px breakpoint, where the runway also has to fit @fold9's end-of-fill zoom-out (`P7_ZOOMOUT_MS`) before the title block arrives — that duration and this vh are a pair. It was **75/175**, and the last 20vh was dead scroll: measured at 393×852 the zoom-out arms ~400px before `#page-8` ends and lands ~40px into this section at a reading scroll, so the timeline just sat at rest with no card yet on screen. At 55 the card enters the screen exactly as the beat lands and its crossing is still 450px further on, so the ordering the pair exists for is intact. **Don't cut it to the beat's own length** — the runway has to hold at a reading scroll, not just a slow one. The extra height keeps @fold12's card on the house 100vh rhythm, so **change each pair together** |
| `@fold11` | `page-10` | `drawPage8` | לצורך ההשוואה המספרית, כל הריבועים יוצגו בגודל אחיד.<br>ניתן להציג את גודל האירועים באמצעות הכפתור במקרא. | **Size down, then fly** — @fold10's morph played backwards, except it lands in page9's legit zone instead of back on the timeline. `fold11SizeApply` (js/groups.js) off this card's own house 0.5 crossing runs two beats **in order, never blended**: (1) `p7SizeGridSet(true, {uniform:true})` — every square shrinks to the unit size **exactly where it stands**. `p7GridUniform` is a **draw-time override** (`p7GridFlatten`, page7.js), *not* a different pack: the tiered layout stays, each square just shrinks centred in the cell block it already occupies, so **nothing travels on this beat** (a repack at one cell each would move every dot, and the size-down would read as a rearrangement — that was tried and rejected). It is also **not** "the grid off": off means the dots fly home to the timeline. Then (2) after `p7MorphTotalMs()` — a wall-clock `setTimeout`, because the two animations already run on separate clocks; the handle is kept so a reverse crossing mid-beat cancels a flight that hasn't left — `p8Trigger()` sends the whole field into the legit grid. The flatten runs on its **own** four knobs — `P7_FLAT_FLY_MS` / `P7_FLAT_SIZE_MS` / `P7_FLAT_SIZE_START_MS` / `P7_FLAT_TIER_STAGGER_MS` (page7.js:1880-1883), selected by `p7GridMorph.flat` — **not** @fold10's `P7_MORPH_*` set; tuning one fold never moves the other. The tier stagger also runs the OTHER WAY on the flatten: @fold10's grow leads with the top tier, but the flatten leads with **tier 0** (`p7MorphWindows`, page7.js) because the flatten has no position beat and tier 0 is ~12,500 of the ~14,450 dots — leading with the top tier left the whole field frozen for `P7_MAX_TIER × stagger` ms after the crossing, which read as the trigger being broken. That wait is `fold11BeatGapMs()`: `FOLD11_BEAT_GAP_MS` is `null` by default, meaning "strictly after" (the full `p7MorphTotalMs()` clock); set to a number it **overlaps** the beats, and `0` fires the glide with the flatten so the field resizes in flight. Overlapping only reads because the glide's flight origin is `p7GridLiveRect` (page7.js) — the square's **blended** centre + size while `p7GridMorph` is still running, not its flattened rest cell — so the re-pack keeps playing underneath the flight. With the rest cell (`p7GridRestRect`) every overlapping stage snapped straight to the uniform size the frame `drawPage8` took over. That glide is itself **two independent beats on two clocks** (page8.js): `P8_SHRINK_MS` (the square morphing down to the legit-grid size) and `P8_FLY_MS` (its position travelling), staged by `P8_STAGING` — `"together"` (both from 0, each on its own ms — the shipped look: the dots resize *during* flight), `"shrink-then-fly"` (resize in place, then fly at the landed size), `"fly-then-shrink"`. `p8ForwardMs()` is the resulting full traverse and `p8Beats(t)` slices the phase's **raw** progress, re-easing each beat fresh with `p9Ease`; the reverse stays one undivided `P8_REVERSE_DURATION` traverse. Reverse plays both backwards: glide home (`P8_REVERSE_DURATION`), *then* the tiers grow back. `data-page="10"` is `drawPage8`, the bridge glide — the same draw function @fold12 uses — because this fold owns the glide now; @fold12 simply arrives with it already at rest. The **«הצגת גודל האירועים» button** (`p7ScopeBtnEl`, js/groups.js) is a pill parked `P7_SCOPE_BTN_GAP` (22px) above the top row of the right-hand mini-legend, right edges flush (`updateGroups`, js/update-groups.js); it toggles the **tiers**, never the grid. It appears on **this fold's** crossing (the copy above points at it) — and it does **not fade in**: the ring **pops** and the label **types in** behind it (`p7ScopeRevealTrigger`, `P7_SCOPE_RING_POP_MS` 260 + `P7_SCOPE_TYPE_MS_PER_CHAR` 22/char, two windows sliced off the trigger's raw progress, ring on `p7Ease`, characters on `p9Ease` via `typedText`), fired from `fold11SizeApply` so a reverse crossing un-types it. Then it stays for every fold after it, fading out on @fold14's `p9.fold13OutT` clock with everything else; before that it is `hidden` and pointer-events-dead. It is a direct `.layout` child, **not** a `.groups-overlay` one: that overlay is `z-index: 0` and its own stacking context, so a button inside it sits under `.text-col` and never sees a click. **Mobile** gets the same control as a row in the מקרא panel under the groups (`fold6MobileScopeEl`, `.fold6-mlegend-scope`), shown from this fold's crossing on; it runs the same `p7ScopeToggle`, here and on @fold13. The grid and both of this fold's beats run on a phone too |
| `@fold12` | `page-11` | `drawPage8` | פעולות פוליטיות נבדלות זו מזו במטרה, באמצעים ובאופן הפעולה. אבל הגבול בין פעולה לגיטימית לפעולה קיצונית אינו תמיד מוסכם. | The bridge glide's fold, but **not its trigger** — @fold11 fires it (see that row), so this fold is usually reached with the glide already at rest; `page8CheckScroll` (js/page8-9-scroll.js) watches this title only for `.pills-in`, on a crossing that is now a **tunable fraction of the viewport height** — the card's centre passing `window.FOLD12_CARD_FRAC` (`fold12TriggerY`, shipped **0.5**, the house midpoint; smaller holds the fold back). See the `_debug-fold12-trigger.js` harness in [Dev-Workflow](Dev-Workflow.md) — @fold13's pill band arriving a fold early, pills alone, on **both** breakpoints. On mobile they arrive at the band's own `top: 80px` as a **vertical line** — one right-aligned column on the card's own gutter, popping top to bottom, so all ten are visible; top to bottom is the finished row LEFT to RIGHT, so every cabin travels up and to the left and none overtakes a parked one — and @fold13's convoy stands that column back down into the single row. The docked frame itself doesn't move on this fold any more (the drop moved to @fold13's stick), and it is empty here — it carries no resting text at all. The glide itself (`page8.js`) **starts from the flattened size grid, not from the timeline**: each dot leaves its packed cell at its own tier size and shrinks to the legit size across the flight (`p7GridLiveRect`, page7.js), so nothing snaps back onto the timeline in between. `p7SizeGridOnPage` keeps `p7Grid.on` for this page alone; @fold13 snaps it off. The card is **centred**, and the section is **125vh with `padding-bottom: 25vh`** on desktop (`@media (min-width:601px) #page-11`). That pair is one decision: the padding shrinks the centring box back to the first 100vh, so the card still sits at 50vh — the house **100vh from @fold10's card centre** — while `#page-12` starts 125vh in, which is the house 100vh on to @fold13's card, because @fold13's card is **not** centred in its section (it centres 21vh into the desktop 50vh `.page9-title-row`, 29vh higher than 50vh). **Don't top-align this card** (`align-items`/`padding-top`): with @fold10's card filling the first half-viewport, a `flex-start` + `padding-top: 48px` card lands **0.65vh** under it. And **don't trim the height** below 125vh: both cards park on screen together |
| `@fold13` | `page-12` | `drawPage9` | מה נחשב בעיניכם לפעולה לגיטימית, ומה לפעולה קיצונית? | Headline is forced to **one line** at ≥640px (`.page9-title-row .text-card` `width: max-content` + `white-space: nowrap` on the frame, style.css) — it wraps at the 480px `--card-w`; under 640px it wraps as normal. Drag-and-drop categorization. **Desktop section is 150vh, not the house 200vh** (`.page9-title-row` 50vh, sticky still 100vh): the panel engages when the title card sticks and the @fold14 gate is one viewport before `#page-13`, so the 100vh row left ~55vh of pinned, motionless panel before the closing card could rise; the card now sticks ~17vh in and the gate is at 50vh — see [Drag-and-Drop](Drag-and-Drop.md). Desktop runs **layout V2** (`P9_LAYOUT_V2`): bare pill band on top (its pills **pop in right → left** one fold early and **bare — no 6-dot grip handle** — on `.pills-in` at @fold12's crossing; the band's rule, drop zone, pinned header and the **grip handles** follow on `.engaged` at @fold13's stick, each pill widening by 14px around its own dots on the same right→left crest). **Mobile splits the reveal across the same two folds**, same stagger and same `--p9-pop-i`: @fold12 brings the pills alone as a **right-aligned vertical column at the band's own top** (bare — no ⓘ, no selection circle, no band rule, tray still `pointer-events: none`), and @fold13's stick plays **two beats**: the **convoy** first, alone — (`p9TrainToggle`, page9.js) that threads them into the single row — **top down, each cabin first up then to the left**: the top pill slides sideways into place, the one beneath rises to that line and sweeps in behind it, each cabin leaving 90ms after the one ahead of its own 620ms travel, so the line never breaks — and only once it has parked does the rest arrive (the band's rule scaling in from the right, the drop zone, the pinned header, the docked frame's step-down and the pills' own ⓘ + selection square — all gated on `.engaged:not(.training)`), horizontal drop zone under it, pills classified by **drag or click**, legit dots denser in a 150px bottom strip — see [Drag-and-Drop](Drag-and-Drop.md#desktop-layout-v2). The «הצגת גודל האירועים» pill works here: it tiers **the extreme columns and the legit strip** on both breakpoints — on a phone the strip keeps its 54px band and the dots that no longer fit run past the screen edge (`P9_LEGIT_ROOM_M` `"band"`) (`p9ScopeSet` + `p9LegitTierPlan`, page9.js — the strip keeps its height and its dots shrink to fill it, each camp end to end; see [Drag-and-Drop](Drag-and-Drop.md)) on page9's own **strict-fly** timing (land, then grow; 2550ms, `P9_SCOPE_*`), and resets on any page flip. A dot moving between the grids changes size on the way, and that resize is staged: it lands first at its old size and only then resizes, the two beats in sequence, each dot growing over a fixed 450ms the moment it lands (`P9_DROP_SIZE_START` 0.7 / `P9_DROP_SIZE_MS`, page9.js) |
| `@fold14` | `page-13` | `drawPage12` | בתקופה של מחלוקות עמוקות, חשוב שנסתכל מעבר למטרות הפוליטיות של שני המחנות, אלא גם על האמצעים להשגתן.<br>אילו פעולות אנחנו מוכנים לקבל במרחב הציבורי<br>ואילו פעולות חוצות את הגבול. (three title blocks) | The closing statement — **this**, not the credits card, is what @fold13's interaction resolves into. It owns the scroll gate (`p13GateLocked`/`p13GateMax`, unlocked once one @dragcard is extreme; **`let p13TouchBlockOn` must stay declared above the eager `p13SyncGateVisibility()` call** — below it, that call hits the `let` in its temporal dead zone, the throw kills the rest of js/fold11.js, and the wheel/keydown/scroll listeners never register, so the gate silently does nothing and @fold15/@fold16 can be reached with no pill dropped) and **both halves of the hand-off, run back to back across its card's rise**. First the scroll-linked fade-out (`fold13ScrollT`): tray, header, extreme zone, legend, ACLED note, legit dots and counts all fade (and the whole `.frozen` fixed panel goes `pointer-events: none` while faded — it never leaves the screen, and its invisible tray band was eating the hover on @fold15's share buttons) — but the **extreme dots stay standing in their columns, and the camp dividing line stays with them** (it's what those columns sit on; the line rides `fold13ExtremeMorphT`, not `fold13OutT` — see `page9.js`). That fade is compressed into the **first half** of the rise by `FOLD13_FADE_SPAN` (0.5, js/fold11.js), so it is finished by the time the card reaches mid-screen. Then, at exactly that point, `checkFold13` fires the **freeform spread** onto an otherwise-clear screen (`fold13Trigger`, frac 0.5 on this fold's own wrapper). `FOLD13_FADE_SPAN` and that frac are **one decision in two files** — raise the span and the fade runs under the spread. The closing statement is **three title blocks in the one section**: card 1 (everything below — gate, fade, spread — hangs off it) in `.page12-sticky-center`, then two plain `.page13-follow` blocks (100vh each, card centred). The spacing is **solved**, not fixed: `p12SpacingFit()` (page12.js, run at load, on `fonts.ready` and on resize) sets **card 1's wrapper** height to **50vh + half card 1** (50vh is the no-JS fallback in style.css; the wrapper is `position: static`). That is the one value that puts card 2's centre exactly **100vh below card 1's**, card 3 100vh below that and @fold15's centred card 100vh below card 3 — the same centre-to-centre distance as every other consecutive pair of title blocks, whose cards are each centred in a 100vh section. **Never replace it with a fixed vh** — 45vh, 80vh, 100vh, 125vh and a 100vh `padding-bottom` were all tried and each was off by exactly the half-card it ignored. **@fold14 waits directly under @fold13, with no empty run between them, at every width.** The card is pulled flush to the section top (`align-items: flex-start` + `padding-top: 0`). That zero is load-bearing: the gate holds scroll at `#page-13.offsetTop - 100vh` and can't move later (any later and the section is on screen while still locked), so only a card at the very top of its section has its first pixel at the viewport's bottom edge at the exact scroll position the gate unlocks. **Any `padding-top` here re-opens the gap by its own height** — an earlier 25vh did exactly that, and the symptom is easy to misread, because @fold13's own panel is `.frozen` (pinned at top) through the hand-off: with the card still below the fold, the only thing happening for that stretch is an opacity crossfade on a motionless panel. **The fade IS the card's rise:** `fold13ScrollT` measures from the moment the card's top clears the viewport's bottom edge (now the gate line itself) to the moment it reaches its resting spot (`scrollY = #page-13.offsetTop`, flush at the viewport top), and runs on **`p9Ease`** (sine in-out), which finishes at `t=1`, exactly where the card lands. The outro card (@fold16, then directly after this one) is near-full-height, so at the house 100vh it was already climbing into view while this card sat mid-screen. **The two changes are a pair** — extra height under a *sticky* wrapper is hold time, not spacing (a 200vh sticky attempt read as the card being stuck dead centre). None of the scroll machinery cares: `p13GateMax`, `fold13ScrollT` and the `.frozen` threshold all measure `#page-13`'s `offsetTop`, and nothing needs this wrapper to pin any more (`checkFold13` moved off the outro's). An ordinary `.section-title.text-card-frame` title block, wrapped in `.page12-sticky-center` only because `page12StickyEl` — the `gate-hidden` toggle (`p13SyncGateVisibility`) — hangs off that wrapper |
| `@fold15` | `page-14` | `drawPage12` | שתפו עם אחרים | The share block as its own title block. An ordinary house fold — `#page-14` is 100vh, card centred; with `#page-13`'s three cards spaced 100vh apart (card 1's wrapper solved to 50vh + half card 1) and `#page-15` centring the outro in its first viewport, the three card centres sit exactly 100vh apart, like every other consecutive pair. A plain `.text-card-frame` card (`.page12-share-card`, rtl, centred): the «שתפו עם אחרים» `.section-title` (the shared 20px/16px rule, no override) over the button row `.page12-share` (`#page12Share`: **round icon buttons** — WhatsApp / X / Facebook / LinkedIn share intents + copy-link, inline SVG glyphs in `currentColor`, hrefs wired by `p12ShareInit` in page12.js; the copy button swaps its link glyph for a check and its tip to «הקישור הועתק» for 1600ms. Each shows its platform name on hover as a small dark label above it (`::after { content: attr(data-tip) }`) — in English (WhatsApp / X / Facebook / LinkedIn), except the copy button's Hebrew «העתקת קישור». Button size `--share-icon` (36px), glyph `--share-glyph` (50%); the look is one class on the row — `.is-outline` (ring, inverts instantly on hover), `.is-filled`, `.is-brand` (per-platform `--brand`) — `.is-outline` is the shipped look; centred flex row, **12px gap**, 16px under the title; **no hairline** — the row is the card's only content). **Its own trigger, `fold14PairTrigger`** — as the card reaches mid-screen the camps pair off: @fold14's spread is still camp-**split** (left events left of `W/2`, right events right of it), and this second move dissolves that divide. Every visible dot lerps from its freeform target to a **couple slot** — two dots `P12_PAIR_GAP` (**3px**, tuned) apart laid out like a **domino**, laid out as a **weave**: orientation alternates like a checkerboard (`(col + row) % 2`), so every couple lies across its four neighbours — flat, upright, flat. **Not** a random share of upright couples (the removed `P12_PAIR_VERT`): random reads as noise, the criss-cross reads as woven fabric, which is the point — on a slot grid over the whole width (`p12EnsurePairTargets`, page12.js). The slot is **square** (`SPAN + 2*margin` where `SPAN = 2*SQ + P12_PAIR_GAP`) so a flat and an upright domino are interchangeable in any slot. The margin is **derived from the inside gap**, never independent of it: `max(CELL − SQ, P12_PAIR_GAP) * P12_PAIR_SPREAD` (2.3) — the moment the outside gap stops beating the inside one the pair stops reading as a pair and the field reads as plain grain, which is exactly what happened when the margin was a flat `2*gap` and the inside gap was raised to 3. Slots `p7Shuffle`d with seed 54321, and the grid is **full-bleed** — the leftover px are spread into the cell pitch (`stepX = W / cols`, `stepY = H / rows`, couple centred by `padX`/`padY`) so it spans the whole screen instead of leaving a bare strip down the right and along the bottom. The fold shows a **fixed number of dots**, `P12_DOT_COUNT` (**9250**, tuned) — a target, not a cap: the couple count is simply `min(slots that fit, floor(P12_DOT_COUNT / 2))`, unclamped by the data. Short of it, the leftover slots become **newcomers** that grow in; over it, the surplus real dots **shrink away** in place during the pop beat, on the same clock and in the same size-only vocabulary (`SQ * (1 − popT)`) — never a fade. **A couple is always one dot from each camp** — slot A left, slot B right (**removed — don't reintroduce:** the "any dot with any dot" pool). Each camp's list is `p7Shuffle`d first (seeds 11111 / 22222): the event lists arrive grouped by actor, so taking index k of each in order handed whole runs of couples the same two groups. The constants are `var` — they were tuned live through a `manual/` + `compare/` harness, since removed — and `p12EnsurePairTargets` caches on `(W, H, gap, spread, count)`, so a write is picked up on the next `draw()`. **The dots do not keep their group colour.** Across the fly beat each one lerps (`p12MixColor`) from its own group colour to a **hash-picked colour from the whole six-group roster**, fillers included, so the finished field is a full mix rather than two camp-coloured halves. The two colours of a couple are picked together (`flyPair(k)`, B out of the roster **minus** A) and are **never the same** — a same-colour domino reads as one group sitting with itself, the opposite of what the couple is for — colour is a secondary attribute and may run on its own timing, position may not.

**Two beats**, sliced off the trigger's **raw** progress (`currentRaw()`, not the already-eased `currentT()`) with `p9Ease` re-applied per window. They are timed **independently in ms** — `FOLD14_POP_MS` (775) and `FOLD14_FLY_MS` (2240), js/fold11.js: the trigger's duration is a *function*, `fold14TotalMs()` (makeTrigger resolves a function duration per frame), and the split point is `fold14PopSpan()` = pop's share of the total. Named exceptions to `GROUP_TRANSITION_MS` because the beats are deliberately uneven — quick pop, long flight; **pop** — the newcomers grow in *on their own camp's side* of @fold14's still-split spread, **free across that half** — `px`/`py` are a `p12Rand(n, salt)` hashed x and y anywhere in the half (`halfX0` = 0 or `W/2`, spans `W/2 − SQ` by `H − SQ`), snapped to nothing. Two earlier schemes both read as pattern and are **removed — don't reintroduce**: hanging each newcomer off an existing dot clumped them around their hosts ("shapes"), and handing each one a `CELL`-sized cell out of a `p7Shuffle`d cell list showed the lattice through any amount of sub-cell jitter — at ~7,100 newcomers over ~11,500 cells that is 62% occupancy. Placement must come from a hash, never from `n * a % b` on the running index — modular arithmetic walks in lockstep with the slot order and the newcomers pop in along visible **diagonal streaks**; then **fly** — the whole field, newcomers included, travels to the couple slots. A filler's pop position (`px`/`py`) is independent of `spreadPosOf` (the @fold14 spread alone, @fold15 not applied) so it doesn't inherit its home dot's pair flight. `p9.fold14PopT` sizes the newcomers, `p9.fold14PairT` moves everything. The camps have different counts, so a slot with no real dot left gets a **decorative filler** — not a real event, not in `p9.lastPositions`, not hoverable. **A newcomer always belongs to the camp of its slot** (slot A left, slot B right) and pops in on that camp's own half, so **each camp fills its own shortfall** and both halves grow newcomers whenever both camps are short. **Removed — don't reintroduce:** trimming both camps to the smaller one so the two newcomer counts come out exactly equal — with 4293 vs 332 visible dots that discarded ~3961 real dots and invented 91% of the field. Each camp has its **own** palette and its **own** palette seed (13579/24680) and its own pop-position salts, the colours taken from that camp's **whole roster** (`p7.leftEvents`/`p7.rightEvents`) and *not* from the visible subset — at this point only one category has been dropped "above", so a camp's visible dots are usually all one group and the palette collapses to a single colour (the all-green fillers bug). It **enters by size** (`SQ * popT`, centred in its half), never by opacity — dots never fade. Fully reversible: scrolling back up walks the couples home and shrinks the fillers away. `drawPage12` keeps painting @fold14's spread extreme dots behind all of it. **Removed — don't reintroduce here:** the drawn-in event map (`drawFoldMap`/map.js, `foldMapTrigger`) over this runway — snapshot commit `834ee0d`, see [Data](Data.md) |
| `@fold16` | `page-15` | `drawPage12` | קיצוניים משני הצדדים (outro) | Outro/credits card — an **ordinary title block** whose title is the **one documented per-page `.section-title` size exception** (`#page-15 .section-title`: 40px desktop / 28px mobile, style.css), and the one card that is near-viewport-tall (`100vh − 96px`, width solved by `p12CardWidthFit()`), so it is **placed by its top edge, not centred**. Centring it in a 100vh section is the recurring bug: it leaves only `50vh − half the share card + 48px` of air under @fold15, well short of the house gap, because with a card this tall the centre-to-centre rhythm and the *edge* gap stop agreeing. What reads as the house distance between two title blocks is the air between card **EDGES** — `100vh` minus a title card. So `p12SpacingFit()` (page12.js) sets `#page-15`'s `padding-top` to `50vh − half the share card` (where an ordinary centred card's top would land, making the measured gap exactly `100vh − share card` ≈ 784px at 900vh) and its `min-height` to `padTop + card + P12_OUTRO_END` (48px, matching the card's own 48px viewport inset), so the document ends 48px under the card. That needs the card **in flow**: `@media (min-width: 601px)` makes this section's `.page12-sticky-center` `position: static; height: auto` — a sticky wrapper ignores the padding and re-centres, which is what the gap regresses to every time. Mobile keeps the flowing wrapper and gets no solve (both properties are removed under 600px). **No trigger of its own** — the freeform spread and the camp divider's fade both fire back on the closing statement (`checkFold13`), so the dots have already spread by the time this card rises. Under the methodology the card carries a four-line credits paragraph (`.page12-credits`: research/design, mentor, Shenkar thanks, data — the data line links out as `acleddata.com`; one `<br>` per line; centred, 14px/1.5, 0.04em tracking, 70% black, under a hairline divider). **The hairline sits in the middle of its gap** — a 38px gap split 19px above the line (`margin-top`) and 19px below it (`padding-top`). The card already fills a 900px-tall viewport, so `@media (max-height: 920px)` also trims that spacing (9px/9px — still centred). **Removed — don't reintroduce:** the fixed **drawer** (`p13Drawer`/`P13_OPEN_MS`, the `position: fixed` card, the docked `peek`, the hover slide-up and the `#p13Back` button) and the map's wheel-zoom/drag-pan at the dock — all scaffolding for the event map, removed with it (snapshot commit `834ee0d`). |

Symbol names (`fold6Trigger`, `drawFold7`, `page7TitleCardEl`…) carry **older, unrelated
numberings** and do not line up with `@foldN`. Don't infer a fold from a symbol name.

## Trigger → card wiring

Every fold trigger is armed by `watchCardThreshold(cardEl, frac, trigger)` — `frac` is
the fraction of viewport height the card's top must cross. A **bigger** `frac` is an
**earlier** crossing. `frac` may be a function, re-read on every check, for a fold whose
crossing point differs by viewport.

| Trigger | Card | `frac` |
|---|---|---|
| `fold2Trigger` | `#page-1 .text-card` | **0.5** (`FOLD2_CARD_FRAC`, same on mobile). On desktop `#page-1`'s card is pulled up to `padding-top: 10vh` AND the section is shortened to `min-height: 67vh` by the same ~33vh the card moved (style.css), so the card enters while the hero title is still leaving while the @fold2→@fold3 distance stays exactly the house rhythm; every other fold keeps the shared 100vh centered section. Don't reintroduce: pulling MORE cards up (only relocates the gap to the next centered fold) or shrinking sections below 100vh (two title cards end up on screen together — one card alone needs a gap ≥ 100vh, i.e. section ≥ 100vh + card). |
| `fold3Trigger` | `#page-2 .text-card` | **0.6 on mobile** (`FOLD3_CARD_FRAC`), 0.5 desktop |
| `fold6Trigger` | `#page-3 .text-card` | **0.18 on mobile** (`FOLD6_CARD_FRAC` — a *late* crossing, well below the house 0.5, so the card is almost all the way up before the hand-off starts and its tail plays as @fold5 comes up; tuned by eye on 2026-09-14), 0.5 desktop |
| `squaresRevealTrigger` | `#page-4 .text-card` | 0.5 |
| `acledNoteTrigger` | `#page-5 .text-card` (both viewports; on mobile it does **not** reopen the מקרא panel — see `fold6MLegendAutoBeat`) | 0.5 |
| `fold7LabelTrigger` | `#page-6 .text-card` | 0.5 |
| `fold8SquareDimTrigger` | `#page-6 .text-card` | same measured crossing as `fold8TooltipTrigger` (`fold8TooltipCardFrac()`) — the dim rides @fold7's own established trigger point, not a separate 0.5, so the 7 non-demo squares dim exactly as the hover demo begins |
| `fold8TooltipTrigger` | `#page-6 .text-card` | **desktop: measured, `fold8TooltipCardFrac()`** — fires when the card's *bottom* has risen `FOLD8_TOOLTIP_CLEARANCE_PX` (**−20px**, tuned by eye — the value is negative, so the card is still 20px short of clearing) past the top edge of where the demo tooltip will appear. Positive fires later, negative earlier. That top is derived live from square 0's rect the same way `fold8PositionTooltip` derives it (the square's **top edge** − 5px gap − the box's height, measured at the grown size), and the box's height is measured off a hidden offscreen clone carrying the demo event's real text (`fold8MeasureTooltipHeight`, cached, invalidated on resize). Clamped to frac ≥ 0. Falls back to `0.5 − FOLD8_TOOLTIP_ABOVE_PX/vh` (400px) only before events.json has loaded or if the square isn't built. 0.5 mobile. @fold7 trigger #2 — its own crossing, separate from the label crossing, so the tooltip demo never collides with the card |
| `fold8DemoGrowTrigger` | `#page-6 .text-card` | same crossing as `fold8TooltipTrigger` (`fold8TooltipCardFrac()`), but its own tempo: it runs for `FOLD8_DEMO_GROW_DELAY_MS` (0) + `FOLD8_DEMO_GROW_MS` (350ms) and the swell reads a `{start, len}` window of its **raw** progress through `fold8DemoGrowT()`. So the square leads and the tooltip follows it by `FOLD8_TOOLTIP_DELAY_MS` (250ms). Drives square 0's rest side, `FOLD8_DEMO_GROW_PX` (14px, js/groups.js) |
| `fold9Trigger` | `#page-7 .text-card` | 0.5 |
| `checkFold9LegendPeek` | `#page-7 .text-card` | 0.5 — @fold8's own crossing. Not a `makeTrigger`: a trigger-shaped stand-in (`{set, trigger}`) calling `fold9LegendPeek()`, which auto-plays the legend's **hover** state so the title's «ניתן לסנן קבוצות באמצעות המקרא» demonstrates itself. Fires `fold6LabelHoverTrigger` to 1 (the labels only — the ACLED note is deliberately left closed), waits `FOLD6_LABEL_HOVER_MS` + `FOLD9_LEGEND_PEEK_HOLD_MS` (2000ms hold, timed from when the type-in lands, not from the crossing), then back to 0. Desktop only; skipped entirely, and its pending collapse cancelled, while a real pointer is over the legend (`fold6LegendPointerOver`); scrolling back up cancels the peek and collapses |
| `fold9FlyTrigger` | `#page-7 .text-card` | `fold9FlyFrac()` — desktop 0 (card fully offscreen); **mobile** the card ALMOST out, its bottom within `P7_AXIS_INTRO_CARD_REMAIN_PX_MOBILE` (40px) of the top edge, i.e. the year axis's own draw-in line, so the squares leave exactly as the axis starts drawing |
| `fold13Trigger` | `#page-13 .page12-sticky-center` (@fold14's own wrapper) | **0.5** — the halfway point of the closing card's rise (frac 1 = its top at the viewport bottom, 0 = at the top), i.e. the moment it reaches mid-screen. Kept in lockstep with `FOLD13_FADE_SPAN` (js/fold11.js), which ends the fade there so the spread lands on a clear screen. Watched on **@fold14**, not @fold15 — the spread is the closing statement's flourish. Never use frac 0 on the outro's wrapper (@fold16): it is the last section and exactly one viewport tall, so its top only reaches 0 at the document's final scrollable pixel and the morph effectively never played. |
| `fold14PairTrigger` | `#page-14 .text-card` (@fold15's share card) | **0.5**, the house crossing — the share card reaching mid-screen pairs the camps off (`updateFold14`, js/fold11.js). Duration is a **function**, `fold14TotalMs()` = `FOLD14_POP_MS + FOLD14_FLY_MS` (775 + 2240), so the two beats are timed independently and a live edit lands on the next crossing. Its raw progress is sliced into **two windows** at `fold14PopSpan()`, `p9Ease` re-applied fresh per window: `p9.fold14PopT` (newcomers grow in on their own camp's side) then `p9.fold14PairT` (the whole field flies to the couple slots). Rides on top of `fold13Trigger`'s spread, which is fully played by the time this fires; `p12PairTargets` is invalidated on the first frame so the slot grid is built at the current `W`/`H`. |

## Notable per-fold details

**@fold2 — camp headers type in.** `FOLD2_BEATS` (js/groups.js) slices `FOLD2_ENTRANCE_MS`
(2400 ms) into `shrink` `{0, .198}`, `move` `{.073, .708}`, `headerCoalition`
`{.677, .219}`, `headerChange` `{.781, .219}`. The two headers have their **own** beats,
so one camp can start before the other.

**@fold4 — the headers un-type instead of traveling (both viewports; on mobile the camp
names reappear as static headings inside the מקרא panel — see
[Groups-and-Legend](Groups-and-Legend.md#the-mobile-מקרא-bar)).** The mini-legend's two columns
carry no camp titles (explicit instruction). The headers stay exactly where @fold2 put
them and play their typing backwards: each header's `FOLD2_BEATS` window is *mirrored*
inside `fold6Trigger` (`start → 1-(start+len)`) and its progress inverted, so the camp
that typed in last disappears first. Retiming the entrance automatically retimes the
exit — there is no second pair of constants. Opacity only ramps over the beat's first
quarter (`Math.min(1, t*4, untype*4)`) so the first/last characters don't pop.

**@fold8 — the 8 squares fly to real dots, permanently.** `fold9FlyTrigger`
(`FOLD9_FLY_MS` 1500) colors each square by its own actor and flies it to the real
per-event dot it stands in for. The real cascade never draws its own dot for those 8
events (`p7GetClaimedEvents`), so the DOM square simply stays. The fly is independent of
`p7HasEngaged` — both fire off the same crossing. `draw()` runs unconditionally during
the fly so a fast scroll into `#page-8` doesn't strand it.
The tooltip holds `FOLD9_TOOLTIP_SHRINK_DELAY_MS` (500 ms) after square 0 lands, then
shrinks over `FOLD9_TOOLTIP_SHRINK_MS` (400 ms); reversing un-latches immediately and
cancels the pending timer.


## Mobile status

The 600px breakpoint and what it changes are documented in
[Architecture](Architecture.md#mobile--responsive). Per-fold state:

| Fold | Mobile |
|---|---|
| `@fold1` | ✅ Hero is center-relative already; the logo inset was tightened, the title capped/shrunk to 32px, and the whole hero (title, subtitle **and** both dot columns) pulled down by `PAGE0_MOBILE_DROP` = 1 dot step = 17px — `page0DotBaseOffsetY()` in `page1.js` plus the two `top:` overrides in `style.css`'s ≤600px block. All three must move together or the pixel-measured title→column / subtitle→column gaps break |
| `@fold2`–`@fold4` | ✅ Camp gap is a fixed 90px between the blocks' facing edges (`campCenterGapPx`); labels shrink + wrap to two lines, matching the Figma mobile frame. Wrapped labels forced @fold3 onto its own measured row steps (`fold3RowStep` — equal visible gaps, whatever each label's line count) — see [Architecture](Architecture.md#mobile--responsive). At `@fold4` the six rows **and both camp headers fly into the open מקרא panel**, reshaping to its swatch/type/one-line labels on the way, and the panel's own rows take over in a single frame as they land (a swap, not a cross-fade — the landing is pixel-exact) (`window.FOLD4_FLY`, default on; setting it `false` from the console gets the older hand-off, where the rows don't travel at all — swatch shrinks in place, label un-types on mirrored `@fold3` windows) — the headers land on the panel's own camp headings, leaving only the מקרא button (with `FOLD4_FLY` off they un-type like desktop instead) |
| `@fold6`–`@fold8` | ✅ Fractional `SBB` geometry; tooltips clamped to the viewport. **There is no on-canvas mini-legend** — it and the camp names collapse into the מקרא card pinned to the top of the viewport (the ACLED credit does not: on mobile it is a bare `acleddata.com` link in the opposite, top-left corner) — a floating ACLED-note card (tint, no border or shadow) whose closed pill sits in the **top-right** corner, right edge flush with the title blocks, and which grows downward out of that pill when opened. It out-stacks the title blocks (1006) — at this edge it shares their space, and a control the reader has to press cannot render behind the copy. `@fold4`'s hand-off **closes it itself** `FOLD6_MFLY_CLOSE_GAP_MS` after the rows land, `@fold5` **closes it** as well (its grey squares are the subject; an open panel covers them) and it **stays closed** from there — `@fold6` deliberately does *not* reopen it (its ACLED card is the subject); the credit link appears in the top-left corner on that crossing instead (`fold6MLegendAutoBeat`; see the trigger table's mobile note on `acledNoteTrigger`); see [Groups-and-Legend](Groups-and-Legend.md#the-mobile-מקרא-bar) |
| `@fold9`–`@fold13` | ✅ Camps stay mirrored side-by-side; the dot size is **solved per viewport** (`p7SolveMobileSq`) — the largest square whose grid still holds the bigger camp, ~1.35 at 320×568 up to ~2.4 at 600 wide — inside a box whose top/bottom are px clearances off the docked tooltip and the year axis (`sbbTimeline(H)`). Axis margin, type sizes and label wrapping have mobile values; the hover layer is **off** on mobile, replaced by the **event picker** — a press-and-hold anywhere on the chart opens a loupe you drag over the dots (`p7InspectInit`; the docked frame carries no resting hint — see [Timeline](Timeline.md)). See [Timeline](Timeline.md#mobile). `page9.js` is 1.5px on mobile, so page8's bridge lerp usually *shrinks* dots across the glide (the solved ~1.35–2.4 → 1.5) rather than growing them as it does on desktop — on the narrowest phones, where the solve lands under 1.5, it grows them slightly instead |
| `@fold13` | ✅ **Tap to classify** — no dragging on mobile. A pill never leaves the tray: tapping toggles `.is-extreme` on it (the pill fills light grey, its square solid black) and commits the same state change dragging would, so the FINALIZED "state 1" drop animation is reached untouched. `#page9ZoneAbove` and the drag handle are `display:none`; in the handle's place (the pill's **right** edge in RTL) each pill grows a 12px **selection square** (`.page9-pill-check`, hard corners) — an empty outline at rest, filled solid black on the pill's light-grey selected fill once `.is-extreme` (no ✓ glyph — the fill is the signal) — and on its left an 18px **ⓘ button** that opens the category description (`#page9CatTooltip`), the touch stand-in for the desktop pill hover, handled in the capture phase so it informs without classifying. The circle is decorative (`pointer-events: none`, `aria-hidden`): the whole pill stays the one tap target. At @fold12 the pill **fits its text content**; both of these expand it at @fold13, desktop's grip-handle treatment — and they land as the fold's **last** beat, once the convoy has parked, so their width animation can't reflow the row under a cabin still in flight** — see [Drag-and-Drop](Drag-and-Drop.md#two-beat-reveal--pills-in-fold12-then-engaged-fold13). Mobile also gets **its own headline and subtitle** — «מהי פעולה קיצונית בעיניכם?» (`.copy-mobile`, project.html) and «סמנו פעולות הנחשבות לקיצוניות בעיניכם» (`P9_SUBTITLE_MOBILE`) — naming the mark, not the drag. Geometry swaps under `p9Metrics()`/`p9MidY()`/`p9ExtremeTopY()`: dot 1px, extreme pitch 2px, and the legit half is currently a **desktop-style spread strip** (`P9_LEGIT_SPREAD_M`, 54px tall, free shuffled 1px dots on a finer 1.5px pitch — under review); the Figma 290-409 **4px bar per camp** (solid, groups as contiguous colour segments, shrinking from its outer end) sits behind that flag — see [Drag-and-Drop](Drag-and-Drop.md#the-legit-bar). The count labels draw as the **bare number only** (no "אירועים"), and the tooltip→grid clearance reserves a label line's worth of room (`P9_COUNT_LABEL_ROOM_M` 20, inside `p9ExtremeTopY`) so even a full-height column leaves the line its own band under the docked frame; the divider stroke draws at `p9MidY` like desktop. Hover is off, replaced by the same press-and-hold loupe as `@fold9`. See [Drag-and-Drop](Drag-and-Drop.md#mobile) |
| `@fold14` | ✅ Short title card. Its wrapper is `position: static` at every width (the desktop `#page-13` override; the mobile `#page-15` override doesn't reach it), so it reads the same on a phone, dead-scroll trim included: the house 100vh section, card flush to its top, and the fixed מקרא bar + docked event frame that occupy that top edge are on `fold13ScrollT`'s fade, so they are gone by the time the card lands there. |
| `@fold15` | ✅ Plain title block; the share buttons wrap to a second line inside the card as needed. |
| `@fold16` | ✅ Outro card width + title size clamped; body copy 14px/1.55 and the wrapper (static at every width now) grows to `height: auto; min-height: 100vh` — the methodology text is taller than a phone viewport, so it scrolls instead of clipping. That override is scoped `#page-15 .page12-sticky-center` on purpose — @fold14 shares the class and must not grow. The document ends 48px under the card at every width. |

### @fold13's tray on mobile

Under the breakpoint (`style.css`) the tray is **not a bottom sheet** — Figma node 294-1272
pins it as a full-bleed **band directly under the title card** (`top: 80px`, `bottom: auto`),
so @fold13's mobile stack reads top-to-bottom as **legend → title → pill band → docked tooltip
frame → dot grid → legit bar**. The band goes full-bleed (`width`/`max-width: 100vw`,
overriding the base rule's `width: max-content`), carries a rule on its **bottom edge only**
(`1px solid rgba(90, 90, 90, 0.18)` — nothing divides it from the subtitle above, which reads as
one header block with it), and hides by
sliding **up** off the top edge
(`translate(-50%, calc(-100% - 116px))` — its own height plus the 116px offset).
`80` is mirrored by `P9_TRAY_TOP_M` in `page9.js`, which derives both the tooltip's drop spot
and the grid's top from it, and by `js/fold11.js`'s slide-out; change one and you change all
three. Its padding is a tightened
`8px 0 10px` with a 10px gap (Figma's `23px 0 24px`/14 read too airy on device), and
`.page9-tray-title` ("סוגי פעולות") is **`display: none`** — the node has no label on the band,
the subtitle above already naming the gesture. It's hidden rather than dropped from
`p9BuildPanel` so desktop, which still shows it, keeps one code path.

**The title and subtitle are flushed right in the stuck state**, not centered (same node) —
but the flush is the *pinned* look, not the card's look. While the block is still scrolling
up it is an ordinary centered title like every other fold's, and it travels to the edge only
once it pins at the top, alongside the border fade. Everything below is scoped to
`.page9-title-row` and to the ≤600px query; desktop and every other fold stay centered
throughout.

`text-align` can't do the flush — `.text-card` already inherits `text-align: right`, and what
centers the title is the *box*: `.text-card-frame` is `width: fit-content; margin: 0 auto`.
Releasing that auto margin (`margin-inline: 0 auto`) would flush it, but **`auto` is not an
animatable value**, so the box could only snap sideways the frame `.is-stuck` lands. Instead
the frame stays centered and `.is-stuck` applies `transform: translateX(var(--p9-title-flush))`,
which joins the existing 0.35s transition and glides.

`--p9-title-flush` is written by **`page9UpdateTitleFlush`** (`js/page8-9-scroll.js`), called
from `page9UpdateFromScroll` just before the class toggle: half the slack between the card's
`clientWidth` and the frame's own zero-padding width. It needs JS because the frame is
`fit-content` — the distance depends on how the title happened to wrap at this viewport.
It measures **only while unstuck**: the value is needed before the class lands, and the
padding is mid-transition for 0.35s afterwards. Subtracting the *live computed*
`paddingInlineStart` rather than a hard-coded 29 keeps the reading self-consistent at any
point of that transition, so a reverse crossing can't poison it either.

The frame's 29px side padding is still zeroed in `.is-stuck` on top of the shift — that's the
state Figma shows and the one where the dashed border has faded — so the text ends up ~15px
off the glass rather than 53.

The subtitle sits under it on `margin-top: -16px`, against the base rule's `-21px` (which cancels
the frame's bottom padding outright and leaves the two line boxes overlapping by 3px — desktop
keeps that). The **title→subtitle gap is specified at 2px**, and the margin is derived from it:
the frame contributes `16px padding-block + 2px border` below the title's line box, so
`18 + margin = 2` → `-16px`. Change the spec, change this to `gap - 18`. It's a line-box
measurement — optically, title descender to the subtitle's cap height, it reads ~9px larger.

The pill run is **one pill tall and scrolls horizontally** (Figma node 293-947). Both
`.page9-tray-row` wrappers become `display: contents`, so all 10 pills are hoisted into
`#page9ZoneBelow` — flipped from the base rule's flex *column* to a single `flex-wrap: nowrap`
row with `overflow-x: auto` — and form one continuous run that overflows both screen edges.
The document is RTL, so the run starts scrolled to its right end: the first pill sits at the
right edge and the rest continue off to the left. The scrollbar is hidden
(`scrollbar-width: none` + `::-webkit-scrollbar`); the pill cut off at the edge is the
affordance. `.page9-pill` needs `flex-shrink: 0` + `white-space: nowrap` or flex squeezes the
run to fit and the labels wrap to two lines.

Because nothing has to fit the viewport any more, the pill is its Figma size (node 294-1279):
16px Assistant SemiBold, `padding: 8px 10px`, 6px gap — the height comes from
`line-height: 12px`, reproducing Figma's cap-height text-box trim, so 16px glyphs paint outside
their line box. It carries **no `box-shadow` in any state** — the base rule's rest/hover/
`.dragging` lifts are all zeroed under the breakpoint (all three selectors repeated, since
`.page9-tray .page9-pill:hover` outranks a bare `.page9-pill`): the node is a flat chip, hover
has no meaning on touch, and dragging is replaced by tap. The tray's own side padding is **0** so the run reaches the glass; the end
inset lives on `#page9ZoneBelow` instead, inside the scroller, so it scrolls with the content.
That inset is **logical and asymmetric**: `padding-inline: calc((100vw - min(480px, 100vw - 48px)) / 2) 12px`.
In an RTL document `start` is the right edge — the end the run rests at, and the only pill edge
the reader sees at rest — so it's set to the title card's own gutter, putting the first pill
exactly under the flush-right title. The gutter is re-derived from `--card-w`'s own `min()`
rather than hard-coded as 24, because between 528px and 600px the card stops growing and the
gutter widens. The far (left) end keeps a plain 12px; it scrolls off screen.

Two knock-on values in that stack:

- **`--card-top` is `24px` under the breakpoint** (base: `4.4vh`), manual/-baked 2026-09-14 once the
  מקרא card stopped sitting above the title; the tray band follows at `top: 80px` (`P9_TRAY_TOP_M` 80).
  It's flat px because the
  thing being cleared is itself fixed-px. The gap the eye actually reads is not `60 − 52`: once
  the card sticks its frame is transparent, so the measurement is bar-bottom to the first line
  of title *ink*, a further 2px border + 16px `padding-block` down — ≈22px.
  `page9UpdateFromScroll` does not hard-code a fraction —
  it reads `getComputedStyle(page9TitleCardEl).top` back off the card, so the
  stick threshold follows the variable at either breakpoint and the card can't jump as it sticks.
  The card's natural top is computed exactly from the title row's measured box
  (`rowTop + (rowH − cardH)/2`), **never** approximated as `innerHeight * 0.5`: `innerHeight`
  is the *visual* viewport while the row is `100vh` (the large viewport), and on mobile the
  two disagree by the browser-bar height exactly while scrolling up (bars showing) — the
  approximation released `.is-stuck` ~100px before CSS sticky let go, pinning the
  white-filled dashed frame at the top of @fold13 in its un-stuck styling.
- **The extreme grid's rows are clipped at `midY`, not `H - 16`** (`drawBandedCols`, `page9.js`).
  On desktop the two are equivalent; on mobile `midY` is `H` minus the 4px legit bar, so the old
  floor culled the bottom rows and opened a ~12px gap between the dot columns and the bar they
  should rest on.

Dissolving the wrappers drops `P9_TRAY_GRID`'s hand-tuned slots entirely — order falls back to
plain DOM order (row 1's five, then row 2's five), and each pill's inline `grid-column` is
inert in a flex container. `p9MeasureTrayLayout` writes `""` for `gridTemplateColumns` and the
row `height` on mobile — cleared, not merely skipped, so crossing the breakpoint on a resize
can't strand a desktop track list. Desktop is untouched (the tray still measures 828×198 at
1440px).

With the tray at the top, the legit bar sits **flush with the viewport's bottom edge**
(`gridTopY` = `H - p9LegitBarH(W)`) and `p9MidY` is simply `H - p9LegitBarH(W)` — nothing is
below it any more.

**The docked tooltip frame moves to make room.** The band lands in the upper part of the
screen, where the frame has to end up on this fold, so a third dock spot was added (the
@fold8/@fold9 rest is at the *bottom* of the viewport — `tooltipDockRestPx()`, see
[Timeline](Timeline.md) — so this is a move **up** and then down onto the band):
`p9DockTopM()` = `P9_TRAY_TOP_M + p9TrayH() + P9_TRAY_TOOLTIP_GAP_M` (20) — measured off the
live band, so the frame stays glued to it however the pills size. It runs on
`p9TooltipDropTrigger` (`js/groups.js`, 850ms, matching `.page9-tray`'s own slide), fired from
`page9UpdateFromScroll`'s `isStuck` crossing (`js/page8-9-scroll.js`) — @fold13's own stick,
the same one that engages the band — so the frame steps down exactly as the band's chrome
arrives. **It deliberately does NOT ride @fold12's crossing** (where it used to, back when the
band itself only arrived at @fold13): per explicit instruction the docked frame keeps the
resting spot it has on @fold11 for the whole of @fold12, which now brings the pills alone. It's set on
every tick rather than only on the crossing (`trigger()` early-returns at rest, so that's
free), which also resolves it on the first tick and keeps it latched while scrolled past. It
blends into
`tooltipDockTopPx` via `tooltipDockDropPx` so all three spots stay one continuous lerp. It's a
trigger and not a CSS `transition: top` because that `top` is already rewritten every frame by
the @fold7→@fold8 dock handover, which a transition would smear.

`p9ExtremeTopY` then trails the frame's **hint line**, not the frame: `p9DockTopM() + P9_HINT_H_M (19) + 20 + label room` —
a picked event's frame (and its "עוד" expansion) overlays the column tops instead of reserving 100px.

The category tooltip (the ⓘ popover) flips **below** its pill whenever there's no room above
— which at the top of the screen is always — with `.is-below` moving its arrow to the box's
top edge.

On @fold14's exit (`js/fold11.js`) the tray does not slide out — it fades in place with
every other fold element (inline opacity over `fold13ScrollT`, same as the header, zone,
legend and title card), on both breakpoints. The mobile מקרא bar fades with them too —
its layer (`fold6MobileLegendLayerEl`) sits outside `groupsOverlayEl`, so `js/fold11.js`
writes the same opacity onto it by name. The shared `#page9Tooltip` (`fold8TooltipEl`)
gets the same treatment — on mobile it's the docked event frame, which otherwise sat
fully visible while everything around it faded. `js/fold11.js` clears that inline opacity **once**, on the way back to `eScroll` 0 (`fold13TooltipFaded`), and never again while it stays at 0: `updateFold13` runs on every scroll tick from the top of the page, so clearing it every time wiped @fold7's grow-in opacity between frames, and the docked frame strobed as it opened. Because two other writers also set that
element's inline opacity, every writer multiplies in `1 - p9.fold13OutT`: the sequence
rAF's grow-in opacity (`js/fold8-tooltip.js`), `updateGroups`' show branch
(`js/update-groups.js`), and the mobile picker's `sync()`/`showEvent()` (page7.js) —
`sync()` in particular runs on every redraw/scroll while `currentPage` is still 9, so
its unconditional `"1"` fought the fade every frame (visible stutter). Without the
shared factor any one of them stomps the fade back to full opacity between scroll
ticks and the frame snaps/stutters instead of fading. The frame's
`keepEmptyFrame` lifetime bound (`js/update-groups.js`) is `currentPage <= 12`, not 9:
the IntersectionObserver flips `currentPage` to 10 partway through @fold14's scroll-in,
and a 9 bound made `forceHide` reset the frame (`display:none`) at that flip point
mid-fade — the fold11 scroll fade owns the frame's exit instead.

The extreme dots' freeform spread (`drawPage12`/`p12EnsureFreeformTargets`, page12.js)
draws at `p9Metrics().SQ` and, on mobile, spreads at `p9Metrics().CELL` — the same size
and pitch the dots had in @fold13's extreme grid (1.5px on 2px there); desktop keeps its
original `P7_CELL` spread pitch with 3px dots.

Hover-only affordances (the @fold12 square dim, the axis hover states) simply never activate
on touch. That's safe — they convey no information that isn't otherwise available. The one
that *did* carry unique information, the timeline's per-event tooltip, now has a touch
equivalent: the mobile event picker on `@fold9` (see
[Timeline](Timeline.md#the-mobile-event-picker)).
